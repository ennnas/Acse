int a, b;

a = 1;
b = 0;

protect {
    write(a/b); // if b==0 jump to 13
    protect{
        write(a/(b-1)); // if b==1 jump to 10
    } with {
        a = a/(a-b); // if b==a jump to 13
    }
} with {
    protect {
        write(a/(b+1)); // if b==-1 jump to 17
    }
}
write(a);
write(b);